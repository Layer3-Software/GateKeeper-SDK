export interface ModalProps {
  kycType: string[];
  account: string;
  geoIds?: string[];
  modalColor?: string;
}

interface ModalTextProps {
  header: string;
  title: string;
  description: string;
}
